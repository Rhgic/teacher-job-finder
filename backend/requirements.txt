sqlalchemy>=2.0,<3.0
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
httpx>=0.27          # DeepSeek / 微信登录 / 附件下载
weasyprint>=61       # 简历 PDF 渲染
pypdf>=6.0           # 简历 PDF 文本抽取（填充 structured_content）
python-docx>=1.1     # 简历 Word(.docx) 文本抽取
python-multipart>=0.0.9  # FastAPI 接收 multipart 上传
pytest>=8.0          # 单元测试
ruff>=0.6            # 代码检查，规则见 ruff.toml（CI 与本地同一套）
alembic>=1.13        # 数据库迁移，脚本在 migrations/
pymysql>=1.1         # MySQL 驱动（生产数据库）
redis>=5.0            # 限流计数、每日配额、LLM 结果缓存
cryptography>=42.0   # MySQL 8 默认 caching_sha2_password 认证需要，缺了连不上
# 后续阶段按需启用：
# psycopg[binary]>=3.1     # PostgreSQL
# cos-python-sdk-v5>=1.9   # 腾讯云 COS 上传（render_to_pdf 的 _maybe_upload_cos）
